# Scaling Checklist — {PROJECT_ID}

**Date**: {YYYY-MM-DD HH:MM UTC}
**Environment(s)**: {env list}
**Direction**: {Upscale | Downscale | Mixed}
**Manifest**: `./{{PATH_DOCS}}/5-deployment/resource-manifest.json`

{If ANSIBLE_IMPACT is true — include this block at the top, before all other sections}:

> ## ⚠ ANSIBLE PROJECT WILL REQUIRE RE-EXECUTION AFTER APPLY
>
> **The following scaling changes affect resources that your Ansible project manages:**
> - {resource_id} — {reason: e.g., "desired_capacity change alters running instance count" / "instance_type change alters host hardware spec"}
>
> **After `terraform apply` completes you MUST re-run Ansible against the affected hosts, or your infrastructure will be misconfigured:**
> - New or replaced instances will not have been provisioned with application config, packages, or services.
> - If Ansible uses a static inventory, it will be stale and must be updated to reflect the new instance IPs.
> - If Ansible uses a dynamic inventory, re-run it immediately after apply to pick up new hosts.
>
> **Ansible project path**: `{{PATH_INFRA}}/deployment/ansible/`

{If STALE_WARNING is true — include this block, otherwise omit it entirely}:
> **Stale manifest warning**: The manifest was out of sync with the Terraform project at the time this checklist was prepared. The user chose to proceed without reconciling. Changes below may not fully reflect the current deployed state.

---

## Changes

| # | Resource ID | Type | Environment | Property | Current Value | Proposed Value | Change Type |
|---|---|---|---|---|---|---|---|
| 1 | `{id}` | {resource type} | {env} | `{property}` | `{current}` | `{proposed}` | {In-place \| Requires replacement \| Destructive} |

**Change types:**
- **In-place** — Terraform updates the resource without destroying it. No downtime.
- **Requires replacement** — Terraform must destroy and recreate the resource. Downtime likely.
- **Destructive** — permanently reduces availability, HA, or data protection (e.g., removing Multi-AZ, reducing NAT gateway count below per-AZ).

---

## Risk Assessment

{One bullet per change of type "Requires replacement" or "Destructive" — omit this section entirely if all changes are In-place}:

- **`{resource_id}`.`{property}`**: {description of risk and expected impact — e.g., "RDS instance class change requires a stop/start; brief downtime expected during terraform apply."}

{If no risks}:
No destructive or replacement-required changes. All changes are in-place.

---

## Constraints and Warnings

{Include only if soft warnings were raised during validation in Phase 8.3 — omit section entirely if none}:

- **{constraint name}**: {description and recommendation}

---

## Summary

- **Resources scaled**: {N}
- **Properties changed**: {N}
- **Terraform files to be modified**: {comma-separated list}

---

*Generated by deployment-infra-terraform skill — Phase 8 Scaling Request*
